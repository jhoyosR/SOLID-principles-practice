/*
public class Intern extends Employee {

    protected int dailyWorkTime;
    protected int monthlyWorkDays;
    
    public Intern (String id, String name, int hourPayment, int dailyWorkTime, int monthlyWorkDays) {
        super(id, name, hourPayment);
        this.dailyWorkTime = dailyWorkTime;
        this.monthlyWorkDays = monthlyWorkDays;
    }

    @Override
    public void work() {
        System.out.println("Intern " + name + " is now working...");
    }

    public void rest() {
        System.out.println("Intern " + name + " is now off-work...");
    }

    @Override
    public void calculateSalary() {
        int salary = hourPayment * dailyWorkTime * monthlyWorkDays;
        System.out.println("Intern " + name + " salary: $" + salary);
    }

}
*/